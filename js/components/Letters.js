import React, { Component, PropTypes } from 'react';
import { Point, Container } from '../sharedStyles';

export default class Letters extends Component {
  static propTypes = {
    word: React.PropTypes.string,
  };

  convertWordToPoints = () =>

  render() {
    return (
      <Container>
        <Point />
      </Container>
    );
  }
};